apiVersion: v1
kind: Service
metadata:
  name: frontend-service-canary
  namespace: unibooker
spec:
  selector:
    app: frontend
    version: canary
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP